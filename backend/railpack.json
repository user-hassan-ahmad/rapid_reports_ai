{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "cd /app/backend && poetry config virtualenvs.in-project true",
        "cd /app/backend && poetry install --no-interaction --no-ansi --only main"
      ]
    }
  }
}
