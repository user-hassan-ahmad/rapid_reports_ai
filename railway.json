{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "export PYTHONPATH=/app/src:$PYTHONPATH && poetry run python fix_migration_state.py && poetry run uvicorn rapid_reports_ai.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

