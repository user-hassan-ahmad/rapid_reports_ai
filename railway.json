{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export PYTHONPATH=/app/src:$PYTHONPATH && cd /app/backend && poetry run python run_migration.py && cd /app && poetry run uvicorn rapid_reports_ai.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

